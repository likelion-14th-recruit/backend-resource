name: cd-main.yml

on:
  push:
    branches:
      - main

jobs:
  cd-main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Main 서버 로그인, 이미지 PULL 및 배포
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VDI_HOST }}
          username: ${{ secrets.VDI_USER }}
          password: ${{ secrets.VDI_PASSWORD }}
          script: |
            cd /home/ubuntu/deploy
            chmod +x deploy.sh
            ./deploy.sh